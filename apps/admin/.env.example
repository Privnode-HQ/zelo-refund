# Optional. If not set, the admin UI will use API Key login (sent as Bearer token).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# API base URL (leave empty when using nginx proxy in Docker)
VITE_API_BASE_URL=http://localhost:3001
